# Native tools this project's asset build needs.
#
# Install with:
#   brew bundle install
#
# This file used to be dominated by the wasm toolchain — emscripten, cmake and
# jq existed only to compile the C puzzle engine and post-process its output.
# `retire-c-engine` removed all of it; the games are TypeScript and the only
# generated asset left is the manual.

# Doc generation for the in-app manual (scripts/build-manual.sh).
brew "halibut"

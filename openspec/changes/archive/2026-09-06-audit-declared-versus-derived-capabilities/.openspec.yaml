# The survey ran and came out with a decision, so there is now something to
# state normatively: how a shared mechanic is joined, how a guard finds its
# population, and how the three surviving boolean declarations are held to the
# behavior they claim.
schema: spec-driven

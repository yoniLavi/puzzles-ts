# record-the-letter-run-no-go — tasks

Implemented 2026-09-12.

- [x] 1.1 Record the alphabet measurement in `src/engine/run-length.ts`'s
      header, beside the grammar paragraph that already refuses these games:
      the count, the date, the four conventions, and the reason a parameterized
      helper would be three knobs plus a seam.
- [x] 1.2 Record the same in `docs/games/engine-catalog.md`
      § "`run-length.ts` — the desc grammar eight games share", since that is
      the menu an author reads before re-rolling.
- [x] 1.3 No `src/` behavior changes, and no spec delta. The gate must show a
      documentation-only diff.

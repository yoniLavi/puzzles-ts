# No deltas yet: this change's first task is an exploration, and Sixteen's
# drag-to-slide may end the direction outright. A requirement written before
# that test would be a spec for something that may not ship.
schema: spec-driven
skip_specs: true

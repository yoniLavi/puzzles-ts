# characterize-the-hint-assessment-corpus — tasks

Scaffolded by `re-derive-the-fixpoint-no-gos`; rescoped 2026-09-04 after the
owner clarified that the hintless games are a deliberate assessment corpus
rather than a gap. Not started.

## 1. Establish the real list

- [ ] 1.1 Do **not** carry the proposal's 19 forward as a finding. Read each
      candidate's solver and classify it by shape: deduction ladder / search /
      movement-objective planner.
- [ ] 1.2 Record what the grep over-counted, with the reason, rather than
      silently dropping it (the `adopt-shared-deduction-fixpoint` precedent).
- [ ] 1.3 Carry a vacuity guard: state how many solvers were read.

## 2. Characterize, so the corpus is an instrument

- [ ] 2.1 Per game, what it would press on: board model (edge/vertex/bespoke
      geometry), substrate (candidate cells, Latin), planner stability, two move
      sets (the Galaxies shape), difficulty tiers that mean something.
- [ ] 2.2 Per game, what already exists to build on — a recording solver path, a
      reason type — versus what would have to be built.
- [ ] 2.3 Per game, whether its generator already accepts only boards its
      techniques can explain; if not, the narratable-deduction policy applies
      first and its boards may move.

## 3. Recommend an assessment order

- [ ] 3.1 Name the two or three to write hints for first, and **what each would
      falsify** about the framework contract if it went badly. A corpus entry
      that cannot fail anything is not an assessment.
- [ ] 3.2 Hand the top pick to the `find`/`apply`/`narrate` exemplar change.

## Findings

_(none yet — not started)_

# Deltas after the extraction, not before: the requirement should state what the
# module actually promises across its three layers, and the second game is what
# decides that.
schema: spec-driven
skip_specs: true

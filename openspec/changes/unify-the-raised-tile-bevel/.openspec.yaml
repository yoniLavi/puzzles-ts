# No delta yet: the requirement is worded from the existing recessed-border one,
# and the sentence it needs — what the helper takes and what stays at the call
# site — is settled by the extraction rather than before it.
schema: spec-driven
skip_specs: true

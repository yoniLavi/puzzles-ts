# share-the-desc-digit-fact — tasks

Not started. Read `proposal.md` first; its table is a grep, not a reading.

- [ ] 1.1 Re-measure by shape — every digit char-code literal in a non-test
      game source that is *not* against the button (the input half is already
      guarded) — and read each site, classifying it into the proposal's four
      shapes and noting where the desc's alphabet differs from
      `desc-alphabet.ts`'s.
- [ ] 1.2 Decide per shape: adopt `c2n`/`n2c`, add a decimal scanner beside
      `run-length.ts`, or leave a site with a stated reason. Record it in
      `design.md`.
- [ ] 1.3 Adopt, game by game. Every frozen differential passes unedited; a
      fixture that moves is a finding, not a re-baseline.
- [ ] 1.4 Widen `emittable-keys.test.ts` § 3 from "against the button" to
      "anywhere in a game source" once the population allows, and prove the
      widened guard fails on a planted site.
- [ ] 1.5 Catalog whatever was added (`docs/games/engine-catalog.md`) and
      update the guide (`docs/games/mechanics.md`, the desc codec section).

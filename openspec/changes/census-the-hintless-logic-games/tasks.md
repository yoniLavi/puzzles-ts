# census-the-hintless-logic-games — tasks

Scaffolded by `re-derive-the-fixpoint-no-gos`, which found the gap. Not started.

## 1. Establish the real list

- [ ] 1.1 Do **not** carry the proposal's 19 forward as a finding. Read each
      candidate's solver and classify it by shape: deduction ladder / search /
      movement-objective planner.
- [ ] 1.2 Record the games the grep over-counted, with the reason, rather than
      silently dropping them (the `adopt-shared-deduction-fixpoint` precedent:
      its "~29" became 1 adoption + 6 read no-gos + ~25 explicitly unaudited).
- [ ] 1.3 Carry a vacuity guard: state how many solvers were read.

## 2. Cost each one

- [ ] 2.1 Per deductive game: does its generator already accept only boards its
      techniques can explain? If not, the narratable-deduction generation policy
      applies first and its boards may move — say so, per game.
- [ ] 2.2 Note which already have the raw material (a recording solver path, a
      reason type) versus which need one built.

## 3. Propose an order

- [ ] 3.1 Scaffold one change per game, cheapest-first, each citing this census.
- [ ] 3.2 Loopy specifically: it is the one whose *bespoke-loop obligation* is
      currently recorded as unmet, so it has a reason to be early that the
      others do not.

## 4. Put the decision in front of the owner

- [ ] 4.1 "Nothing ships hintless" — adopt now, adopt later, or adopt with named
      exemptions? Player-visible, so the owner's call; this change's job is to
      make it a decision about a measured list rather than a vibe.

## Findings

_(none yet — not started)_

# Hint deixis sweep

Generated by `npx vitest run -c scripts/checks/diff.vitest.config.mts hint-deixis`.
**Advisory.** Read the file's header before acting on a row: most rows are
legitimate ties this lexical filter cannot recognise, and the question to ask
of each is *are the two marks the same kind of thing?*

3914 steps examined, 3045 of them displaying a second mark;
230 distinct sentence shapes flagged across 20 games.

| game | sentence shape |
| --- | --- |
| boats | This segment is a boat's left end, so its boat must continue into the square to its right. |
| boats | Row # still needs one more ship and has just one free square left, so that square must hold a boat segment. |
| boats | Column # still needs one more ship and has just one free square left, so that square must hold a boat segment. |
| boats | Filling this square would make a boat of #, and the largest one still missing is # — so it must be water. |
| boats | Filling this run would make a boat of #, but every #-boat is already placed — so the free square must be water. |
| boats | If this square held a boat segment, it would complete a boat the fleet has no room for — so it must be water. |
| boats | If this square were water, it would complete a boat the fleet has no room for — so it must hold a boat segment. |
| crossing | This run is # squares long, and only one number in the list is # digits — so it must be #. |
| crossing | Only one #-digit number left matches the digits already in this run, so it must be #. |
| dominosa | This square can pair with only the #–# domino, so it must go here. |
| dominosa | Every remaining spot for the shaded domino covers this pair, so no other domino can go here. |
| dominosa | A #–# domino here would force a second #–# at the shaded square — but each domino is used once — so it can't. |
| dominosa | A #–# domino here would force both shaded squares to become #–# too — a duplicate — so it can't. |
| dominosa | A domino here would split the empty squares into two odd-sized regions, and an odd region can't be filled by dominoes — so this can't be a domino. |
| dominosa | The shaded square can only be part of the #–# domino, so #–# can't sit here instead. |
| filling | The shaded region of # can't fully grow without this square. |
| filling | The shaded region of # has only this one empty square to grow into. |
| group | In this row, e can go in only this cell — every other cell in the row has ruled it out — so it must be e. |
| group | In this row, f can go in only this cell — every other cell in the row has ruled it out — so it must be f. |
| group | In this row, d can go in only this cell — every other cell in the row has ruled it out — so it must be d. |
| group | In this row, b can go in only this cell — every other cell in the row has ruled it out — so it must be b. |
| group | In this row, a can go in only this cell — every other cell in the row has ruled it out — so it must be a. |
| group | In this row, c can go in only this cell — every other cell in the row has ruled it out — so it must be c. |
| keen | No way to make this cage sum to # leaves room for # in this cell, so we must cross out #. |
| keen | No way to make this cage multiply to # leaves room for # and # in this cell, so we must cross out # and #. |
| keen | No way to make this cage have a ratio of # leaves room for # in this cell, so we must cross out #. |
| keen | In this row, # can go in only this cell — every other cell in the row has ruled it out — so it must be #. |
| keen | There's already a # in this row and column, so we must cross out the # from the other cells they pass through. |
| keen | No way to make this cage differ by # leaves room for # in this cell, so we must cross out #. |
| keen | No way to make this cage sum to # leaves room for # and # in this cell, so we must cross out # and #. |
| keen | No way to make this cage multiply to # leaves room for # in this cell, so we must cross out #. |
| keen | No way to make this cage multiply to # leaves room for #, # and # in this cell, so we must cross out #, # and #. |
| keen | No way to make this cage differ by # leaves room for # and # in this cell, so we must cross out # and #. |
| keen | In this column, # can go in only this cell — every other cell in the column has ruled it out — so it must be #. |
| keen | No way to make this cage have a ratio of # leaves room for # and # in this cell, so we must cross out # and #. |
| keen | This cage must multiply to #, and every way to fill it places a # in this row — so the # here must be crossed out. |
| keen | This cage must multiply to #, and every way to fill it places a # in this column — so the # here must be crossed out. |
| keen | This cage must sum to #, and every way to fill it places a # in this column — so the # here must be crossed out. |
| keen | This cage must differ by #, and every way to fill it places a # in this row — so the # here must be crossed out. |
| keen | Following a chain of two-candidate cells, placing # here would force a contradiction further along — so we must cross out #. |
| netslide | Row # never slides, so only a column move can shift this corner: take it to row #, where it belongs. |
| palisade | …and this edge can't be a wall either. |
| palisade | Clue # already has all its walls, so this edge can't be one. |
| palisade | Joining these regions would exceed the target size, so this edge must be a wall. |
| palisade | This region is too small and can only grow this way, so this edge can't be a wall. |
| palisade | A wall can't stop in mid-air at this corner, so this edge must be a wall. |
| palisade | Clue # needs all its remaining edges to be walls, so this one must be a wall. |
| palisade | …and this edge must be a wall too. |
| pattern | This column's run of # can slide only # cells, so this cell must be black. |
| pattern | This column's run of # can slide only # cells, so these cells must be black. |
| pattern | This row's run of # can slide only # cells, so this cell must be black. |
| pattern | This column's run of # has nowhere to slide, so this cell must be black. |
| pattern | No run can reach this cell in this column, so it must stay white. |
| pattern | This row's run of # can slide only # cells, so these cells must be black. |
| pattern | This row's run of # has nowhere to slide, so this cell must be black. |
| pattern | This row's run of # has nowhere to slide, so these cells must be black. |
| pattern | No run can reach this cell in this row, so it must stay white. |
| pattern | This row's run of # can slide only # cell, so these cells must be black. |
| pattern | No run can reach these cells in this column, so they must stay white. |
| pattern | This row's run of # can slide only # cell, so this cell must be black. |
| pattern | This column's run of # can slide only # cell, so this cell must be black. |
| pattern | No run can reach these cells in this row, so they must stay white. |
| pattern | This column's run of # has nowhere to slide, so these cells must be black. |
| pattern | This column's run of # can slide only # cell, so these cells must be black. |
| salad | The clue to the right of this row sees B first, and this is the nearest square to it — so nothing but B can go here, and we must cross out A and C. |
| salad | The clue to the left of this row sees A first, and this is the nearest square to it — so nothing but A can go here, and we must cross out B and C. |
| salad | In this row, B can go in only this square — every other square in the row has ruled it out — so it must be B. |
| salad | There's already a B in this row and column, so we must cross out the B from the other squares they pass through. |
| salad | This row already has its one empty square, so every other square in it must hold a letter. |
| salad | This column already has its one empty square, so every other square in it must hold a letter. |
| salad | Every other letter has been ruled out in this square, so it can only be C. |
| salad | There's already a C in this row and column, so we must cross out the C from the other squares they pass through. |
| salad | Every other letter has been ruled out in this square, so it can only be A. |
| salad | There's already an A in this row and column, so we must cross out the A from the other squares they pass through. |
| salad | Every other letter has been ruled out in this square, so it can only be B. |
| salad | All # letters of this column are already placed, so every other square in it must be empty. |
| salad | The clue to the right of this row sees C first, and this is the nearest square to it — so nothing but C can go here, and we must cross out A and B. |
| salad | The clue to the left of this row sees C first, and this is the nearest square to it — so nothing but C can go here, and we must cross out B. |
| salad | The clue to the right of this row sees A first, and this is the nearest square to it — so nothing but A can go here, and we must cross out B. |
| salad | In this row, C can go in only this square — every other square in the row has ruled it out — so it must be C. |
| salad | In this row, A can go in only this square — every other square in the row has ruled it out — so it must be A. |
| salad | The clue to the right of this row sees C first, and this is the nearest square to it — so nothing but C can go here, and we must cross out B. |
| salad | The clue to the left of this row sees C first, and this is the nearest square to it — so nothing but C can go here, and we must cross out A and B. |
| salad | The clue to the right of this row sees B first, and this is the nearest square to it — so nothing but B can go here, and we must cross out A. |
| salad | Following a chain of two-candidate squares, placing C here would force a contradiction further along — so we must cross out C. |
| salad | The clue to the left of this row sees B first, and this is the nearest square to it — so nothing but B can go here, and we must cross out A and C. |
| salad | The clue to the right of this row sees C first, and this is the nearest square to it — so nothing but C can go here, and we must cross out A. |
| salad | In this column, C can go in only this square — every other square in the column has ruled it out — so it must be C. |
| salad | In this column, B can go in only this square — every other square in the column has ruled it out — so it must be B. |
| salad | All # letters of this row are already placed, so every other square in it must be empty. |
| salad | Following a chain of two-candidate squares, placing A here would force a contradiction further along — so we must cross out A. |
| singles | This # shares a line with the ringed white #, which already uses that number — so this copy must be shaded. |
| singles | These #s share a line with the ringed white #, which already uses that number — so they must be shaded. |
| slant | The same clue forces this square too — it must slant toward the clue. |
| slant | The same clue forces this square too — it must slant away. |
| slant | Two corners of this square are already joined by a chain of diagonals. Slanting it one way would close that chain into a loop, which isn't allowed — so it must slant the other way. |
| slant | This square is locked to the same slant as the ringed one — the clues around them leave no other pairing — so since that one is a backslash, this must be a backslash too. |
| solo | A # is already placed in this cell, so it can't repeat in the same row, column or block — cross out the # from these cells. |
| spokes | Just enough free spokes left for this hub's count — so they must all be lines. |
| spokes | And this one must be a line too. |
| spokes | Connecting two #-hubs would strand them from the rest — so rule out this spoke. |
| spokes | This hub already has its lines, so the rest can't — rule them out. |
| spokes | And rule this one out too. |
| spokes | Only one free spoke left for this hub's count — so it must be a line. |
| spokes | Drawing this line would over-fill the ringed hub — so rule it out. |
| spokes | Drawing this line would strand the ringed hubs — so rule it out. |
| spokes | Drawing this line would force two diagonals to cross — so rule it out. |
| sticks | The # still needs a longer line, and a vertical line here would leave it room for only # square. So this # must be horizontal. |
| sticks | The # still needs a longer line, and a horizontal line here would leave it room for only # square. So this # must be vertical. |
| sticks | The # still needs a longer line, and a horizontal line here would leave it room for only # square. So this square must be vertical. |
| sticks | A vertical line here would run the #'s line to # squares, longer than its number allows. So this square must be horizontal. |
| sticks | A horizontal line here would run the #'s line to # squares, longer than its number allows. So this square must be vertical. |
| sticks | The # still needs a longer line, and a vertical line here would leave it room for only # square. So this square must be horizontal. |
| sticks | The # rules this square out too — a horizontal line here would leave it room for only # square. So this # must be vertical. |
| sticks | The # rules this square out too — a vertical line here would run its line to # squares. So this square must be horizontal. |
| sticks | A horizontal line here would run the #'s line to # squares, longer than its number allows. So this # must be vertical. |
| sticks | The black # needs a line on each of its # remaining open sides, and a vertical line here would close one off. So this square must be horizontal. |
| sticks | The black # rules this square out too — a horizontal line here would close off another of its open sides. So this square must be vertical. |
| sticks | The black # already has # line running into it, and a horizontal line here would make another. So this square must be vertical. |
| sticks | A vertical line here would run the #'s line to # squares, longer than its number allows. So this # must be horizontal. |
| sticks | The black # already has # line running into it, and a horizontal line here would make another. So this # must be vertical. |
| sticks | The black # rules this square out too — a vertical line here would run into it. So this square must be horizontal. |
| sticks | A horizontal line here would join the # and the # into one line, and a line may carry only one number. So this # must be vertical. |
| sticks | The # still needs a longer line, and a vertical line here would leave it room for only # squares. So this square must be horizontal. |
| sticks | The black # already has # lines running into it, and a horizontal line here would make another. So this square must be vertical. |
| sticks | The black # has just one side left that a line could reach it from, and a vertical line here would close it off. So this square must be horizontal. |
| sticks | The black # needs a line on each of its # remaining open sides, and a horizontal line here would close one off. So this square must be vertical. |
| sticks | The # still needs a longer line, and a vertical line here would leave it room for only # squares. So this # must be horizontal. |
| sticks | The black # has just one side left that a line could reach it from, and a horizontal line here would close it off. So this square must be vertical. |
| sticks | The black # rules this square out too — a vertical line here would close off another of its open sides. So this square must be horizontal. |
| sticks | The # rules this square out too — a horizontal line here would leave it room for only # square. So this square must be vertical. |
| sticks | The # rules this square out too — a vertical line here would leave it room for only # squares. So this square must be horizontal. |
| subsets | The highlighted cell's set lies inside this one, and its C is marked — so C must be here too. Mark C present. |
| subsets | The highlighted cell's set lies inside this one, and its B is marked — so B must be here too. Mark B present. |
| subsets | The highlighted cell's set lies inside this one, and its A is marked — so A must be here too. Mark A present. |
| subsets | Still filling this cell — the highlighted cell's D is marked too, so mark D present here. |
| subsets | Still filling this cell — the highlighted cell's C is marked too, so mark C present here. |
| subsets | This cell's set lies inside the highlighted cell's, which has no B — so B can't be here either. Clear B. |
| subsets | Still filling this cell — the highlighted cell has no D either, so clear D here. |
| subsets | This cell's set lies inside the highlighted cell's, which has no C — so C can't be here either. Clear C. |
| subsets | Only the highlighted sets can still go in this cell. None of them has C — so clear C. For instance, {A} is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It contains D — so mark D present. For instance, {B,D} can't go here — the horseshoe to the highlighted cell needs C present. |
| subsets | Still filling this cell — the highlighted set has no A either, so clear A here. |
| subsets | Only the highlighted set can still go in this cell. It contains A — so mark A present. For instance, {A,B} can't go here — the horseshoe to the highlighted cell needs C present. |
| subsets | Still filling this cell — the highlighted set has no D either, so clear D here. |
| subsets | This cell's set lies inside the highlighted cell's, which has no D — so D can't be here either. Clear D. |
| subsets | The highlighted set can go nowhere but this cell. It contains A — so mark A present. |
| subsets | Still filling this cell — the highlighted set has no B either, so clear B here. |
| subsets | Still filling this cell — the highlighted set also contains C, so mark C present here. |
| subsets | Still filling this cell — the highlighted set also contains D, so mark D present here. |
| subsets | The highlighted set can go nowhere but this cell. It has no A — so clear A. |
| subsets | Only the highlighted sets can still go in this cell. They all contain B — so mark B present. For instance, {A} is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It contains D — so mark D present. For instance, {A,D} is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It contains A — so mark A present. For instance, {A} is already placed on the board (highlighted). |
| subsets | Still filling this cell — the highlighted set also contains B, so mark B present here. |
| subsets | Still filling this cell — the highlighted set has no C either, so clear C here. |
| subsets | Still filling this cell — the highlighted cell's B is marked too, so mark B present here. |
| subsets | This cell's set lies inside the highlighted cell's, which has no A — so A can't be here either. Clear A. |
| subsets | Still filling this cell — the highlighted cell has no C either, so clear C here. |
| subsets | Only the highlighted sets can still go in this cell. None of them has C — so clear C. For instance, {A} can't go here — the horseshoe to the highlighted cell won't allow A. |
| subsets | Only the highlighted set can still go in this cell. It contains C — so mark C present. For instance, {A,B,C} can't go here — the horseshoe to the highlighted cell needs D present. |
| subsets | Only the highlighted set can still go in this cell. It contains C — so mark C present. For instance, {A,C} can't go here — the horseshoe to the highlighted cell needs B present. |
| subsets | Only the highlighted set can still go in this cell. It contains A — so mark A present. For instance, {C} is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It contains B — so mark B present. For instance, {B,D} can't go here — the horseshoe to the highlighted cell won't allow D. |
| subsets | The highlighted set can go nowhere but this cell. It has no B — so clear B. |
| subsets | The highlighted cell's set lies inside this one, and its D is marked — so D must be here too. Mark D present. |
| subsets | Only the highlighted sets can still go in this cell. None of them has A — so clear A. For instance, {A,B} is already placed on the board (highlighted). |
| subsets | Only the highlighted sets can still go in this cell. They all contain C — so mark C present. For instance, {A,B} is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It contains A — so mark A present. For instance, {C} can't go here — the horseshoe to the highlighted cell won't allow C. |
| subsets | Only the highlighted sets can still go in this cell. They all contain C — so mark C present. For instance, {A,C} is already placed on the board (highlighted). |
| subsets | Still filling this cell — the highlighted sets all contain D too, so mark D present here. |
| subsets | Still filling this cell — none of the highlighted sets has B, so clear B here. |
| subsets | Only the highlighted set can still go in this cell. It has no A — so clear A. For instance, {C,D} can't go here — the horseshoe to the highlighted cell needs B present. |
| subsets | Only the highlighted sets can still go in this cell. None of them has B — so clear B. For instance, {A,C} is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It contains A — so mark A present. For instance, {A,C} is already placed on the board (highlighted). |
| subsets | The highlighted set can go nowhere but this cell. It contains D — so mark D present. |
| subsets | The highlighted set can go nowhere but this cell. It contains B — so mark B present. |
| subsets | Still filling this cell — the highlighted cell has no B either, so clear B here. |
| subsets | Only the highlighted set can still go in this cell. It contains B — so mark B present. For instance, {A,B} can't go here — the horseshoe to the highlighted cell needs C present. |
| subsets | Only the highlighted set can still go in this cell. It contains C — so mark C present. For instance, {D} is already placed on the board (highlighted). |
| subsets | Only the highlighted sets can still go in this cell. They all contain D — so mark D present. For instance, {C,D} can't go here — the horseshoe to the highlighted cell needs A present. |
| subsets | Only the highlighted sets can still go in this cell. They all contain A — so mark A present. For instance, {A,C} is already placed on the board (highlighted). |
| subsets | Only the highlighted sets can still go in this cell. They all contain A — so mark A present. For instance, {A} is already placed on the board (highlighted). |
| subsets | Still filling this cell — none of the highlighted sets has C, so clear C here. |
| subsets | Only the highlighted set can still go in this cell. It contains A — so mark A present. For instance, {A,B,C} can't go here — the horseshoe to the highlighted cell needs D present. |
| subsets | Only the highlighted set can still go in this cell. It has no C — so clear C. For instance, the empty set can't go here — the horseshoe to the highlighted cell needs A present. |
| subsets | Only the highlighted set can still go in this cell. It contains B — so mark B present. For instance, {A} is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It contains C — so mark C present. For instance, {A,C} is already placed on the board (highlighted). |
| towers | Clue # sees every tower in this line, so the heights must climb #, #, … straight up from the clue — height # can only sit here. |
| towers | Continuing up the line — height # can only sit here. |
| towers | Clue # sees exactly # towers along this line, so a tower of height # this close to the clue would hide too many towers behind it — we must cross out the #. |
| towers | In this row, height # can go in only this cell — every other cell in the row has ruled it out — so it must be #. |
| towers | Trying every way clue # can show exactly # towers along this line, none of them puts a tower of height # here — so we must cross out the #. |
| towers | In this column, height # can go in only this cell — every other cell in the column has ruled it out — so it must be #. |
| towers | These two clues face each other along this line and add up to one more than the grid, which pins the tallest tower to a single cell — height # can only sit here. |
| towers | Following a chain of two-candidate cells, placing height # here would force a contradiction further along the line — so we must cross out the #. |
| undead | Trace this sightline: a vampire shows before its first mirror, a ghost only after one, a zombie anywhere along it. No arrangement that shows exactly # from one end and # from the other leaves room for the vampire or zombie in this cell — so we must cross out the vampire or zombie. |
| undead | Trace this sightline: a vampire shows before its first mirror, a ghost only after one, a zombie anywhere along it. No arrangement that shows exactly # from one end and # from the other leaves room for the ghost or vampire in this cell — so we must cross out the ghost or vampire. |
| undead | The same sightline rules the ghost or vampire out of this cell too. |
| undead | The same sightline rules the ghost or zombie out of this cell too. |
| undead | Every zombie is already placed, so no undecided cell can be one — we must cross out the zombie here. |
| undead | Trace this sightline: a vampire shows before its first mirror, a ghost only after one, a zombie anywhere along it. No arrangement that shows exactly # from one end and # from the other leaves room for the vampire in this cell — so we must cross out the vampire. |
| undead | The same sightline rules the ghost out of this cell too. |
| undead | Trace this sightline: a vampire shows before its first mirror, a ghost only after one, a zombie anywhere along it. No arrangement that shows exactly # from one end and # from the other leaves room for the ghost or zombie in this cell — so we must cross out the ghost or zombie. |
| undead | Every ghost is already placed, so no undecided cell can be one — we must cross out the ghost here. |
| undead | The same sightline rules the zombie out of this cell too. |
| undead | The same sightline rules the vampire or zombie out of this cell too. |
| undead | Every vampire is already placed, so no undecided cell can be one — we must cross out the vampire here. |
| undead | Trace this sightline: a vampire shows before its first mirror, a ghost only after one, a zombie anywhere along it. No arrangement that shows exactly # from one end and # from the other leaves room for the ghost in this cell — so we must cross out the ghost. |
| undead | Trace this sightline: a vampire shows before its first mirror, a ghost only after one, a zombie anywhere along it. No arrangement that shows exactly # from one end and # from the other leaves room for the zombie in this cell — so we must cross out the zombie. |
| undead | The same sightline rules the vampire out of this cell too. |
| unequal | There's already a # in this row and column, so we must cross out the # from the other cells they pass through. |
| unequal | The cell across this greater-than sign can be no more than #, so this cell must be smaller still — we must cross out #. |
| unequal | In this column, # can go in only this cell — every other cell in the column has ruled it out — so it must be #. |
| unequal | The cell across this greater-than sign can be no less than #, so this cell must be larger still — we must cross out #. |
| unequal | In this row, # can go in only this cell — every other cell in the row has ruled it out — so it must be #. |
| unequal | The cell across this greater-than sign can be no less than #, so this cell must be larger still — we must cross out # and #. |
| unequal | Another group of cells already accounts for a fixed set of numbers that includes #, so we must cross out # here. |
| unequal | The cell across this greater-than sign can be no more than #, so this cell must be smaller still — we must cross out # and #. |
| unequal | Following a chain of two-candidate cells, placing # here would force a contradiction further along — so we must cross out #. |
| unruly | Two of these three cells are already black; a third black would make three in a row, which isn't allowed — so this cell must be white. |
| unruly | Two of these three cells are already white; a third white would make three in a row, which isn't allowed — so this cell must be black. |
| unruly | This column already holds all of its black cells, so every remaining cell in it must be white. |
| unruly | This column already holds all of its white cells, so every remaining cell in it must be black. |
| unruly | This row already holds all of its black cells, so every remaining cell in it must be white. |
| unruly | This row already holds all of its white cells, so every remaining cell in it must be black. |
| unruly | Only one black cell is left to place in this row; anywhere but the ringed cells would force three white in a row, so every other empty cell must be white. |
| unruly | Only one black cell is left to place in this column; anywhere but the ringed cells would force three white in a row, so every other empty cell must be white. |
| unruly | Only one white cell is left to place in this row; anywhere but the ringed cells would force three black in a row, so every other empty cell must be black. |
| unruly | Only one white cell is left to place in this column; anywhere but the ringed cells would force three black in a row, so every other empty cell must be black. |

## Flagged shapes per game

- subsets: 56
- salad: 27
- sticks: 25
- keen: 17
- pattern: 16
- undead: 15
- unruly: 10
- spokes: 9
- unequal: 9
- towers: 8
- boats: 7
- palisade: 7
- dominosa: 6
- group: 6
- slant: 4
- crossing: 2
- filling: 2
- singles: 2
- netslide: 1
- solo: 1

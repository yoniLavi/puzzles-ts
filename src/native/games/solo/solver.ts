import type { SoloState } from "./state.ts";

/** TODO: port the upstream solver's deductions (read puzzles/solo.c). */
export function solveSolo(_s: SoloState): never {
  throw new Error("solo solver: not implemented");
}
